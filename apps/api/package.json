{
  "name": "@mlb-stat-explorer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@mlb-stat-explorer/db": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "fastify": "^5.9.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
