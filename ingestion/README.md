# Ingestion

Python scripts for fetching source data will live here.

For v1, this package will fetch FanGraphs batting season stats through `pybaseball` and write JSON snapshots into `ingestion/data`.
