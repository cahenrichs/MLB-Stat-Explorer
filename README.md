# MLB Stat Explorer

The hitter comparison application uses the MLB Stats API for standard batting totals and manually supplied FanGraphs CSV files for `wOBA`, `wRC+`, and fWAR. See [ingestion/README.md](ingestion/README.md) for the operational import runbook.
